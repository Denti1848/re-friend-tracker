import { IPageElement, IKey } from "../pathinterface";
import { Key } from "../page/element/page-element";
export declare class KeyUtility {
    static translateUrl(url: string, formKey: IKey, crudFormUrl: boolean, parentPageElement: IPageElement): string;
    static findKey(pageElement: IPageElement, name: string): Key;
    static replaceVariable(url: string, variable: string, value: string): string;
    static variableExists(url: string, variable: string): boolean;
}
